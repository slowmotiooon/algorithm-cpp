#pragma once

#include <vector>
using namespace std;

vector<vector<int>> ans;

void dfs(vector<int> nums, vector<bool> used, int i) {}

vector<vector<int>> permute(vector<int>& nums) {
    return ans;
}